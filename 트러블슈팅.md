# Harbor & Voyage - 개발 트러블 슈팅

> 프로젝트 개발 중 발생한 문제와 해결 방법을 기록합니다.

---

## 목차

1. [테마 전환 관련](#1-테마-전환-관련)
2. [스타일링 관련](#2-스타일링-관련)
3. [배포 관련](#3-배포-관련)
4. [성능 최적화](#4-성능-최적화)
5. [라이브러리 이슈](#5-라이브러리-이슈)
6. [TypeScript 관련](#6-typescript-관련)
7. [개발 환경](#7-개발-환경)

---

## 1. 테마 전환 관련

### 1.1 초기 렌더링 시 테마 깜빡임 (FOUC)

#### 문제 상황

- 페이지 로드 시 라이트 모드로 먼저 렌더링된 후 다크 모드로 전환되는 현상
- 사용자 경험을 저해하는 깜빡임 발생

#### 원인

```typescript
// 잘못된 구현
const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState("light"); // 기본값이 항상 light

  useEffect(() => {
    const savedTheme = localStorage.getItem("theme");
    if (savedTheme) {
      setTheme(savedTheme); // 렌더링 후에 변경됨
    }
  }, []);
};
```

#### 해결 방법

```typescript
// 올바른 구현
const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState<"light" | "dark">(() => {
    // 초기값을 함수로 설정하여 첫 렌더링 전에 localStorage 확인
    if (typeof window !== "undefined") {
      const saved = localStorage.getItem("theme");
      if (saved === "light" || saved === "dark") {
        return saved;
      }
      // 브라우저 설정 확인
      return window.matchMedia("(prefers-color-scheme: dark)").matches
        ? "dark"
        : "light";
    }
    return "light";
  });

  useEffect(() => {
    localStorage.setItem("theme", theme);
    document.documentElement.setAttribute("data-theme", theme);
  }, [theme]);
};
```

#### 학습 포인트

- `useState`의 초기값을 함수로 설정하면 마운트 시 한 번만 실행됨
- SSR 환경에서는 `window` 객체 접근 전 체크 필요
- `data-theme` 속성을 HTML 요소에 추가하여 CSS 전환 제어

---

### 1.2 브라우저 테마 감지 후 자동 전환

#### 문제 상황

- 사용자가 브라우저 테마를 변경했을 때 즉시 반영되지 않음

#### 해결 방법

```typescript
useEffect(() => {
  const mediaQuery = window.matchMedia("(prefers-color-scheme: dark)");

  const handleChange = (e: MediaQueryListEvent) => {
    if (!localStorage.getItem("theme")) {
      setTheme(e.matches ? "dark" : "light");
    }
  };

  mediaQuery.addEventListener("change", handleChange);
  return () => mediaQuery.removeEventListener("change", handleChange);
}, []);
```

#### 학습 포인트

- `matchMedia` API를 사용하여 브라우저 테마 감지
- 이벤트 리스너로 실시간 변경 감지 가능
- 사용자가 직접 선택한 테마는 우선순위가 더 높음

---

## 2. 스타일링 관련

### 2.1 Tailwind CSS 다크 모드 적용

#### 문제 상황

- `dark:` 클래스가 작동하지 않음

#### 원인

```javascript
// tailwind.config.js - 잘못된 설정
module.exports = {
  darkMode: "media", // 미디어 쿼리만 감지
};
```

#### 해결 방법

```javascript
// tailwind.config.js - 올바른 설정
module.exports = {
  darkMode: "class", // 클래스 기반 다크 모드
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
};
```

```typescript
// ThemeProvider.tsx
useEffect(() => {
  if (theme === "dark") {
    document.documentElement.classList.add("dark");
  } else {
    document.documentElement.classList.remove("dark");
  }
}, [theme]);
```

#### 학습 포인트

- `darkMode: 'class'` 설정으로 수동 제어 가능
- HTML 요소에 `dark` 클래스 추가/제거로 전환

---

### 2.2 Swiper 슬라이더 스타일 미적용

#### 문제 상황

- Swiper 컴포넌트가 렌더링되지만 네비게이션 버튼, 페이지네이션이 보이지 않음

#### 원인

- Swiper CSS 파일을 import하지 않음

#### 해결 방법

```typescript
// app/layout.tsx
import "swiper/css";
import "swiper/css/navigation";
import "swiper/css/pagination";
import "swiper/css/effect-fade";
```

#### 추가 커스터마이징

```css
/* globals.css */
.swiper-button-next,
.swiper-button-prev {
  @apply text-accent;
}

.swiper-pagination-bullet-active {
  @apply bg-accent;
}

/* 다크 모드 */
.dark .swiper-button-next,
.dark .swiper-button-prev {
  @apply text-accent-dark;
}
```

#### 학습 포인트

- 서드파티 라이브러리는 별도 CSS import 필요
- Tailwind의 `@apply`로 커스터마이징 가능

---

## 3. 배포 관련

### 3.1 Surge 배포 시 404 에러

#### 문제 상황

- 메인 페이지는 정상이지만 새로고침 시 404 페이지 표시
- 직접 URL 입력 시 404 에러

#### 원인

- Surge는 정적 호스팅 서비스
- Next.js는 SPA로 동작하므로 클라이언트 라우팅 필요
- 서버가 모든 경로를 `index.html`로 리다이렉트해야 함

#### 해결 방법

```bash
# 빌드 후 200.html 생성
npm run build
cp out/index.html out/200.html

# Surge 배포
npx surge ./out sparrow-cafe.surge.sh
```

#### 학습 포인트

- `200.html`은 Surge의 특별한 파일명
- 모든 경로를 200.html로 리다이렉트하여 SPA 라우팅 가능
- Netlify는 `_redirects`, Vercel은 자동 처리

---

### 3.2 Next.js Image 컴포넌트 정적 export 이슈

#### 문제 상황

- 빌드 시 에러 발생:
  ```
  Error: Image Optimization using Next.js' default loader is not compatible with `output: 'export'`.
  ```

#### 원인

- Next.js Image 최적화는 서버 사이드 기능
- Surge는 정적 호스팅이므로 동적 이미지 최적화 불가

#### 해결 방법

```javascript
// next.config.js
module.exports = {
  output: "export",
  images: {
    unoptimized: true,
  },
  // 정적 export용 출력 디렉토리
  distDir: "out",
};
```

#### 대안

```typescript
// 직접 img 태그 사용 (작은 프로젝트)
<img
  src="/images/hero.webp"
  alt="Hero"
  loading="lazy"
  className="w-full h-auto"
/>

// 또는 외부 이미지 최적화 서비스 활용
// - Cloudinary
// - imgix
// - ImageKit
```

#### 학습 포인트

- 정적 호스팅에서는 빌드 타임에 모든 것이 결정됨
- 동적 최적화가 필요하면 Vercel, Netlify 등 사용

---

### 3.3 환경 변수 관리

#### 문제 상황

- 로컬에서는 정상이지만 배포 후 API 키가 노출됨

#### 해결 방법

```javascript
// next.config.js
module.exports = {
  env: {
    NEXT_PUBLIC_GOOGLE_MAPS_API_KEY:
      process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY,
  },
};
```

```bash
# .env.local (Git에 포함하지 않음)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_api_key_here
```

#### 빌드 시 환경 변수 주입

```bash
# Surge 배포 전
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_key npm run build
```

#### 학습 포인트

- `NEXT_PUBLIC_` 접두사가 있는 변수만 클라이언트에서 사용 가능
- 민감한 정보는 서버 사이드에서만 사용
- `.env.local`은 `.gitignore`에 추가

---

## 4. 성능 최적화

### 4.1 이미지 로딩 최적화

#### 문제 상황

- 초기 로딩 시간이 5초 이상
- Lighthouse 성능 점수 60점대

#### 해결 방법

**1. WebP 포맷 변환**

```bash
# 이미지 변환 도구 설치
brew install webp

# 변환
cwebp input.jpg -q 80 -o output.webp
```

**2. Lazy Loading**

```typescript
<img
  src="/images/gallery-1.webp"
  alt="Gallery"
  loading="lazy"
  className="w-full h-auto"
/>
```

**3. 반응형 이미지**

```html
<picture>
  <source media="(max-width: 768px)" srcset="/images/hero-mobile.webp" />
  <source media="(min-width: 769px)" srcset="/images/hero-desktop.webp" />
  <img src="/images/hero-desktop.webp" alt="Hero" loading="eager" />
</picture>
```

#### 결과

- 초기 로딩 시간: 5초 → 1.8초
- Lighthouse 성능 점수: 60 → 92

---

### 4.2 폰트 최적화

#### 문제 상황

- 폰트 로딩 중 텍스트가 보이지 않음 (FOIT)

#### 해결 방법

```typescript
// app/layout.tsx
import { Playfair_Display, Noto_Sans_KR } from 'next/font/google'

const playfair = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-playfair',
})

const notoSansKR = Noto_Sans_KR({
  subsets: ['latin'],
  weight: ['400', '500', '700'],
  display: 'swap',
  variable: '--font-noto-sans-kr',
})

export default function RootLayout({ children }) {
  return (
    <html className={`${playfair.variable} ${notoSansKR.variable}`}>
      <body>{children}</body>
    </html>
  )
}
```

```css
/* globals.css */
body {
  font-family: var(--font-noto-sans-kr), sans-serif;
}

h1,
h2,
h3 {
  font-family: var(--font-playfair), serif;
}
```

#### 학습 포인트

- Next.js Font Optimization 활용
- `display: 'swap'`으로 폰트 로딩 중에도 텍스트 표시
- CSS 변수로 폰트 관리

---

## 5. 라이브러리 이슈

### 5.1 Framer Motion 애니메이션 성능

#### 문제 상황

- 스크롤 애니메이션이 끊기고 버벅임
- 모바일에서 특히 심함

#### 원인

```typescript
// 모든 요소에 애니메이션 적용 (비효율적)
<motion.div
  initial={{ opacity: 0, y: 50 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: false, amount: 0.3 }}
>
```

#### 해결 방법

```typescript
// viewport once: true로 한 번만 실행
<motion.div
  initial={{ opacity: 0, y: 50 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, amount: 0.3 }}
  transition={{ duration: 0.6, ease: 'easeOut' }}
>
```

```typescript
// 중요한 요소에만 애니메이션 적용
const shouldAnimate = useReducedMotion() // 접근성 고려

{!shouldAnimate && (
  <motion.div /* 애니메이션 */ />
)}
```

#### 학습 포인트

- `viewport={{ once: true }}`로 재실행 방지
- 접근성: `prefers-reduced-motion` 고려
- 과도한 애니메이션은 성능 저하 원인

---

### 5.2 Lucide React 아이콘 번들 사이즈

#### 문제 상황

- 번들 사이즈가 예상보다 큼
- 모든 아이콘이 번들에 포함됨

#### 해결 방법

```typescript
// ❌ 전체 import (비효율적)
import * as Icons from "lucide-react";

// ✅ 필요한 것만 import
import { Sun, Moon, Menu, X, MapPin } from "lucide-react";
```

#### 학습 포인트

- Tree Shaking을 위해 Named Import 사용
- 번들 크기를 `npm run build` 후 `.next/analyze` 확인

---

## 6. TypeScript 관련

### 6.1 이벤트 핸들러 타입

#### 문제 상황

```typescript
// 타입 에러
const handleClick = (e) => {
  e.preventDefault();
};
```

#### 해결 방법

```typescript
import { MouseEvent } from "react";

const handleClick = (e: MouseEvent<HTMLButtonElement>) => {
  e.preventDefault();
};

// 또는 FormEvent
const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
  e.preventDefault();
};
```

---

## 7. 개발 환경

### 7.1 Hot Reload가 작동하지 않음

#### 문제 상황

- 파일 저장 시 자동 새로고침이 안 됨

#### 해결 방법

```javascript
// next.config.js
module.exports = {
  webpack: (config, { dev }) => {
    if (dev) {
      config.watchOptions = {
        poll: 1000,
        aggregateTimeout: 300,
      };
    }
    return config;
  },
};
```

---

## 정리 및 학습 포인트

### 주요 배운 점

1. **SSR vs CSR**: Next.js의 서버/클라이언트 구분
2. **정적 호스팅 제약**: 동적 기능의 한계 이해
3. **성능 최적화**: 이미지, 폰트, 번들 사이즈 관리
4. **접근성**: 테마 전환, 애니메이션 등 사용자 경험 고려
5. **타입 안정성**: TypeScript로 에러 사전 방지

### 다음 프로젝트에 적용할 점

- [ ] 초기 설정부터 성능 고려
- [ ] 배포 플랫폼 선택 시 기능 제약 확인
- [ ] 라이브러리 선택 시 번들 사이즈 체크
- [ ] 접근성 기준을 프로젝트 시작부터 적용
- [ ] 트러블 슈팅 문서를 개발과 동시에 작성

---

## 참고 자료

- [Next.js 공식 문서](https://nextjs.org/docs)
- [Tailwind CSS 공식 문서](https://tailwindcss.com/docs)
- [Framer Motion 공식 문서](https://www.framer.com/motion/)
- [Swiper 공식 문서](https://swiperjs.com/react)
- [Surge 배포 가이드](https://surge.sh/help/)

---

_최종 업데이트: 2026년 2월 7일_
